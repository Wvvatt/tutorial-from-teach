# ESP32 + OV2640 课程路线

这条路线从已经能运行的串口心跳程序出发，每课只增加一个主要概念。每个阶段都有一个真实设备上的可观察成果和一个小改动，完成后再进入下一课。

> **课程状态：最后一课。** 用户已完成核心拍摄机制的学习目标；第 15 课作为唯一收尾扩展，完整代码已写入固件，完成实机接口验证后结课。第 14 课仍保留为可选工程验收清单。

## 阶段一：建立设备直觉

1. **编译、烧录、串口反馈**
   - 实践：修改心跳周期，编译上传，确认串口和 GPIO2 变化。
   - 通过标准：能解释 `pio run`、`upload`、`monitor` 的边界。
2. **串口输入与时间**
   - 实践：用串口命令改变 LED 闪烁速度，同时保持心跳输出。
   - 通过标准：能区分阻塞式 `delay` 和基于 `millis()` 的定时。
3. **内存与故障反馈**
   - 实践：打印堆内存，制造并观察一次受控失败。
   - 通过标准：能从日志判断程序是否重启、卡住或内存不足。

## 阶段二：把设备放进网络

4. **Wi‑Fi 连接状态机**
   - 实践：连接当前局域网，打印 IP、重试次数和断线状态。
5. **HTTP 状态接口**
   - 实践：浏览器访问 `/health`，读取 uptime、RSSI 和 free heap。
6. **服务器与设备的协议边界**
   - 实践：用一个最小 Python server 接收 JSON，比较 GET、POST 和状态码。

## 阶段三：让 OV2640 工作

7. **识别板型和摄像头引脚**
   - 实践：确认开发板型号、OV2640 排线方向和 D0-D7、XCLK、PCLK 等引脚。
8. **抓取第一张 JPEG**
   - 实践：初始化 `esp_camera`，抓取一帧，串口打印尺寸和字节数。
9. **单张照片 HTTP 接口**
   - 实践：ESP32 提供 `/capture`，浏览器直接显示 JPEG。
10. **实时预览的代价**
   - 实践：比较单张抓拍和 MJPEG stream，观察帧率、内存和网络负载。

## 阶段四：最终项目

11. **间隔拍照上传**
   - 实践：每隔 N 秒向同网段 server `POST /images` 上传 JPEG。
12. **服务端保存与播放**
   - 实践：按时间保存图片，网页按时间顺序播放最近照片。
13. **可靠性与可诊断性**
   - 实践：断线重连、上传超时、失败重试、有限缓存和结构化日志。

## 可选阶段五：端到端验收

14. **使命验收与运行手册**
   - 实践：从正常链路、故障恢复、协议停用、容量上限和系统解释五个角度验证最终项目。

## 最后一课：把作品变成可管理设备

15. **运行时修改上传间隔**
   - 实践：通过 `GET /config` 查看配置，通过带校验的 `POST /config` 修改拍照上传间隔。
   - 通过标准：无需重新烧录即可改变上传节奏；错误输入返回 400 且不污染旧状态。

## 进度

- 阶段一（1–3 课）：已在真实设备上跑通。
- 第 4 课：已掌握协作式轮询与控制权；实际联网成功证据仍需补齐。
- 第 5 课：已掌握请求、路由、响应与 `handleClient()` 的控制权关系；200/404 的设备证据仍需补齐。
- 第 6 课：已开始设备到 Mac 的 JSON POST；201/404/不可达三类证据仍需补齐。
- 第 7 课：已用照片、官方引脚表和实机 PSRAM 输出确认 AI Thinker 兼容硬件配置。
- 第 8 课：已实现摄像头初始化与单帧抓取，掌握帧缓冲的借用所有权；连续三帧实机日志仍需补齐。
- 第 9 课：已实现 `/capture`，掌握 JPEG 响应正文、网络栈缓冲与帧缓冲归还边界；三次实机响应证据仍需补齐。
- 第 10 课：已实现 `/stream` MJPEG 预览；FPS、网络负载和同步处理占用控制权的实机对照仍需补齐。
- 第 11 课：已实现定时 JPEG `POST /images` 与服务端完整性验证；连续上传及停服恢复证据仍需补齐。
- 第 12 课：已实现原子保存、图片列表 API 与时间顺序播放；持续上传时的并发刷新证据仍需补齐。
- 第 13 课：可靠性代码已运行；实机已观察到累计成功 18 次，以及 server 不可达时状态 -1、连续失败 2 次和约 40 秒退避。
- 第 14 课：可选；验收页和运行手册已建立，最终实机验收证据未提交。
- **当前：第 15 课·运行时修改上传间隔。** [完整固件与实机验证步骤](lessons/0015-runtime-upload-configuration.html) 已准备好，PlatformIO 构建通过，等待烧录后的 HTTP 实机证据。
- 拍摄机制的核心学习已记录在 [整体系统模型](learning-records/0008-camera-capture-system-model.md) 中；本课通过后正式结课。

复习可以从 [第 1 课：让设备反馈成为你的调试工具](lessons/0001-serial-feedback.html) 开始；工程运行与排障使用 [运行手册](reference/operations-runbook.html)。
