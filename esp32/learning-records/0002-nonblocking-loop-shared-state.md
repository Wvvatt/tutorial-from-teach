# 非阻塞 loop 与共享状态

用户在设备上跑通了串口命令切换心跳周期，能解释 `intervalMs` 是状态、`lastTickMs` 标记时间边界，并说明长 `delay` 会把控制权锁死，使输入处理、Wi-Fi 和摄像头任务无法共存。此认知是后续所有课程的基础前提：第 4 课的 Wi-Fi 连接必须用 `millis()` 轮询而非 `while + delay`，正因为这条已建立的直觉。