# 协作式轮询与控制权

用户能指出非阻塞设计的核心不是“轮询”这个动作本身，而是每次只检查或推进一点状态，随后把控制权交还给 `loop()`。用户已经在真实设备上修复 `Serial.available() > -1` 导致的无限循环，并观察到修复后心跳与 Wi-Fi 检查可以共同推进。这条判断将直接迁移到 HTTP 服务：`server.handleClient()` 必须被频繁调用并快速返回，不能被等待 Wi-Fi 或长任务阻塞。

